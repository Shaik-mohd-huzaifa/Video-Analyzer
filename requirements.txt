# Core dependencies
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0
huggingface-hub>=0.19.0

# Quantization support (for memory optimization)
bitsandbytes>=0.41.0

# API clients
aiohttp>=3.9.0
requests>=2.31.0
openai>=1.0.0
python-dotenv>=1.0.0

# Vision support (for Qwen-VL models)
Pillow>=10.0.0
torchvision>=0.15.0

# Optional but recommended
sentencepiece>=0.1.99
protobuf>=3.20.0
safetensors>=0.4.0

# For structured output
pydantic>=2.0.0
jsonschema>=4.19.0
